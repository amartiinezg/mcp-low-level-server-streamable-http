# =====================================
# MCP Service Configuration
# =====================================
# IMPORTANT: Copy this file to .env and configure your values
# cp .env.example .env

# Environment
NODE_ENV=development
PORT=3001

# CAP Service URL
CAP_SERVICE_URL=http://localhost:4004

# =====================================
# SAP IAS Authentication (OAuth 2.0)
# =====================================
# Set to true to enable authentication
IAS_ENABLED=false

# JWT Token Validation (for API-to-API auth)
# IAS_ISSUER=https://your-tenant.accounts.ondemand.com
# IAS_JWKS_URI=https://your-tenant.accounts.ondemand.com/oauth2/certs
# IAS_AUDIENCE=your-client-id

# =====================================
# OAuth 2.0 Authorization Code Flow
# (For user login via browser)
# =====================================
# IAS_CLIENT_ID=your-client-id
# IAS_CLIENT_SECRET=your-client-secret
# IAS_REDIRECT_URI=https://mcp-service.c-7c1fc59.kyma.ondemand.com/mcp/callback
# IAS_SCOPES=openid email profile

# Example with full OAuth enabled:
# IAS_ENABLED=true
# IAS_ISSUER=https://mycompany.accounts.ondemand.com
# IAS_JWKS_URI=https://mycompany.accounts.ondemand.com/oauth2/certs
# IAS_AUDIENCE=your-client-id
# IAS_CLIENT_ID=your-client-id
# IAS_CLIENT_SECRET=your-client-secret
# IAS_REDIRECT_URI=http://localhost:3001/mcp/callback
# IAS_SCOPES=openid email profile
