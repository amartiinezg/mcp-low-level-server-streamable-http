apiVersion: gateway.kyma-project.io/v2
kind: APIRule
metadata:
  name: mcp-service-api
  namespace: mcp-cap-integration
  labels:
    app: mcp-service
spec:
  gateway: kyma-system/kyma-gateway
  hosts:
    - mcp-service.c-42fe4ef.kyma.ondemand.com
  service:
    name: mcp-service
    port: 3001
  rules:
    - path: /*
      methods: ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"]
      noAuth: true
